# Worker and staff configuration for watchdog_main.py
# Format: name:slots[:launch_cmd[:cwd]]
# Default launch_cmd: claude --dangerously-skip-permissions
# Default cwd: $REPO_ROOT/<name>
# Resume prompt is auto-generated from the active DB job.

# --- Active workers ---
comfy_render:1:claude --dangerously-skip-permissions:/data/src/bwk/comfy_render
comfy_gnorm:1:claude --dangerously-skip-permissions:/data/src/bwk/comfy_render
mla_attention:1:claude --dangerously-skip-permissions:/data/src/bwk/attention
rmsnorm_backward:1:claude --dangerously-skip-permissions:/data/src/bwk/rmsnorm

[staff]
# Staff agents (pull-based, nudged when idle with open work)
researcher:1:claude --dangerously-skip-permissions:/data/src/bwk/foreman-staff/researcher
